import { Roboto, Inter, Crete_Round } from 'next/font/google'

export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
})

export const roboto = Roboto({
  weight: '400',
  subsets: ['latin'],
})

export const creteRound = Crete_Round({
  weight: '400',
  subsets: ['latin'],
})
